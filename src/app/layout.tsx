import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'
import { Toaster } from '@/components/ui/toaster'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LLMs to PDF - AI Conversation Highlights & PDF Export',
  description: 'Extract, categorize, and export highlighted points from AI assistant conversations to professional PDFs with advanced templates and collaboration features.',
  keywords: 'AI conversations, PDF export, ChatGPT, Claude, Gemini, highlights, collaboration, templates',
  authors: [{ name: 'LLMs to PDF Team' }],
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  )
}
